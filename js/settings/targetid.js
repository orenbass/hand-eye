import { registerSimpleTest } from './general.js';

registerSimpleTest({
  id: 'targetid',
  secondsInputId: 'targetidSeconds',
  difficultySelectId: 'targetidDifficulty',
  minSeconds: 5,
  maxSeconds: 600
});
